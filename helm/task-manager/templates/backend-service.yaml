apiVersion: v1
kind: Service
metadata:
  name: task-manager-backend
  labels:
    app: task-manager-backend

spec:
  type: ClusterIP
  selector:
    app: task-manager-backend

  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
