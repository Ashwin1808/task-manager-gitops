apiVersion: v1
kind: Service
metadata:
  name: task-manager-frontend
  labels:
    app: task-manager-frontend

spec:
  type: ClusterIP
  selector:
    app: task-manager-frontend

  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
