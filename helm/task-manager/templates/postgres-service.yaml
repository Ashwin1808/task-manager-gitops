apiVersion: v1
kind: Service
metadata:
  name: task-manager-postgres
  labels:
    app: task-manager-postgres

spec:
  type: ClusterIP
  selector:
    app: task-manager-postgres

  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
